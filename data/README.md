This repository does not include large/private datasets.

Place small demo images under `data/samples/` for reproducible testing.

Recommended naming for labeled evaluation:
- empty_*.png  (expected EMPTY)
- sign_*.png   (expected CONTENT)
- punct_*.png  (expected CONTENT)
